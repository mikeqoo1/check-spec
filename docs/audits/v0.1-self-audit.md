# Spec/Code Consistency Audit — 2026-05-20-v01-mvp-llm-judged-speccode-consistency-auditor-go-cli-githu

_v0.1 MVP — LLM-judged spec/code consistency auditor (Go CLI + GitHub Action)_

- **Verdict**: REQUEST_CHANGES
- **Model**: claude-opus-4-7
- **Base → Head**: HEAD~1 (1e0254e) → HEAD (36071d0)
- **Files analyzed**: 50

## Summary

The diff implements the bulk of the v0.1 MVP: Go module, full package layout (cmd/check-spec, internal/{arceus,cli,gitdiff,llm,prompt,report,version}), tests including E2E with FakeJudge, GitHub Actions (ci.yml, release.yml, example workflow, action.yml composite), GoReleaser config, Makefile, .golangci.yml, and a substantially expanded README. The diff also includes the .arceus/changes/<id>/ proposal files. Two known-incomplete items remain unchecked in tasks.md (dogfood self-audit and status flip), which match the proposal's stated 'pending user action' gap. Minor drift: docs/audits/v0.1-self-audit.md was added (which the tasks list as pending user execution) containing a pre-filled audit; a LICENSE referenced via .goreleaser archives isn't visible in this diff; and there are a few small expansions not explicitly enumerated (e.g., generic-list-go and other indirect deps, README content above what spec literally itemizes) that are within the documented scope.

## Task implementation (from tasks.md)

| # | Phase | Task | Reported | Actual | Evidence |
|---|-------|------|----------|--------|----------|
| 1 | Phase 0 — Repo bootstrap | 初始化 `go mod init github.com/mikeqoo1/check-spec` | [x] | done | go.mod:1 module github.com/mikeqoo1/check-spec |
| 2 | Phase 0 — Repo bootstrap | 建立目錄骨架：`cmd/check-spec/`、`internal/arceus/`、`internal/gitdiff/`、`internal/llm/`、`internal/report/`、`internal/prompt/`、`testdata/` | [x] | done | cmd/check-spec/main.go, internal/{arceus,cli,gitdiff,llm,prompt,report,version}/ all present; testdata under internal/arceus/testdata/ |
| 3 | Phase 0 — Repo bootstrap | 加入 `.golangci.yml`（啟用 `errcheck`、`govet`、`staticcheck`、`gosec`、`revive`） | [x] | done | .golangci.yml enables errcheck/govet/staticcheck/gosec/revive (+ineffassign/unused) |
| 4 | Phase 0 — Repo bootstrap | 加入 `Makefile`：`make build`、`make test`、`make lint`、`make verify` | [x] | done | Makefile defines build/test/lint/verify targets |
| 5 | Phase 0 — Repo bootstrap | 加入 `.github/workflows/ci.yml`：on push/PR 跑 `make verify` | [x] | done | .github/workflows/ci.yml runs lint+test+build on push/PR |
| 6 | Phase 1 — Arceus loader (`internal/arceus`) | `type Change struct { ID, Title string; Proposal, Spec, Tasks, Decisions string; Meta MetaJSON }` | [x] | done | internal/arceus/arceus.go Change struct with ID, Title, Meta, Proposal, Spec, Tasks, Decisions |
| 7 | Phase 1 — Arceus loader (`internal/arceus`) | `func Load(repoRoot, changeID string) (*Change, error)` — 讀 `.arceus/changes/<id>/{proposal,spec,tasks,decisions}.md` + `meta.json`，缺檔回 error | [x] | done | internal/arceus/arceus.go Load() reads all four .md + meta.json |
| 8 | Phase 1 — Arceus loader (`internal/arceus`) | `func List(repoRoot string) ([]string, error)` — 列出可用 change id（含 archive 排除） | [x] | done | internal/arceus/arceus.go List() excludes 'archive' subfolder |
| 9 | Phase 1 — Arceus loader (`internal/arceus`) | 解析 `tasks.md` 為 `[]Task{Index int, Text string, Checked bool, Phase string}`（保留原始 markdown 也保留結構化） | [x] | done | internal/arceus/parse.go ParseTasks returns []Task{Index,Phase,Text,Checked} |
| 10 | Phase 1 — Arceus loader (`internal/arceus`) | 解析 `spec.md` 的「驗收條件」段：找 `## 驗收條件` 或 `## Acceptance criteria` heading，收集底下 `- [ ]` / `- [x]` 行 | [x] | done | internal/arceus/parse.go ParseAcceptance recognizes both '驗收條件' and 'Acceptance criteria' |
| 11 | Phase 1 — Arceus loader (`internal/arceus`) | 單元測試覆蓋：正常、缺檔、空 acceptance section、混合中英文 heading | [x] | done | internal/arceus/arceus_test.go + parse_test.go cover success/missing/empty/cn-en headings |
| 12 | Phase 2 — Git diff collector (`internal/gitdiff`) | `func Collect(repoRoot, base, head string, maxBytes int) (Diff, error)`，回傳 `Diff{Files []FileDiff, Truncated bool}` | [x] | done | internal/gitdiff/gitdiff.go Collect() returns Diff with Files and Truncated |
| 13 | Phase 2 — Git diff collector (`internal/gitdiff`) | 用 `exec.CommandContext` 跑 `git diff --no-color <base>..<head>` 與 `git diff --name-only`；不可信任使用者輸入 → 預先 validate ref 不含 shell metacharacters | [x] | done | gitdiff.go uses exec.CommandContext; validateRef + refRE reject shell metacharacters |
| 14 | Phase 2 — Git diff collector (`internal/gitdiff`) | 排除 binary file（從 `git diff --numstat` 的 `-` 標記識別） | [x] | done | parseNumstat detects '-' marker as Binary=true; Binary files have empty Diff body |
| 15 | Phase 2 — Git diff collector (`internal/gitdiff`) | 超過 `maxBytes` 時：按 file 切，優先保留 spec/code 相關的非 vendor、非生成檔 | [x] | done | priority() deprioritizes vendor/node_modules/dist/.pb.go/etc; sort.SliceStable used; Truncated flag set |
| 16 | Phase 2 — Git diff collector (`internal/gitdiff`) | 單元測試：使用 `git init` 在 tmp dir 起的 fixture repo，覆蓋一般 diff、binary file 過濾、截斷情況 | [x] | done | internal/gitdiff/gitdiff_test.go uses git init fixture for basic/binary/truncation/invalid ref |
| 17 | Phase 3 — LLM client (`internal/llm`) | 定義介面：`type Judge interface { Verify(ctx, JudgeInput) (JudgeOutput, error) }` | [x] | done | internal/llm/llm.go defines Judge interface with Verify(ctx, Input) (Output, error) |
| 18 | Phase 3 — LLM client (`internal/llm`) | Anthropic 實作 `AnthropicJudge`，使用官方 SDK；spec.md / proposal.md 段落用 `cache_control: ephemeral` 啟用 prompt caching | [x] | done | internal/llm/anthropic.go uses official SDK; buildUserBlocks sets CacheControl for Cache=true segments |
| 19 | Phase 3 — LLM client (`internal/llm`) | Mock 實作 `FakeJudge`：固定 Output 回應（測試與 golden file 用） | [x] | done | internal/llm/fake.go FakeJudge with fixed Output and LastInput capture |
| 20 | Phase 3 — LLM client (`internal/llm`) | `Verify` 要求模型回 JSON；SDK 內建重試機制處理 429/5xx；malformed JSON 由 `ParseOutput` 容錯（去 code fence + 找 `{...}`） | [x] | done | anthropic.go ParseOutput strips code fences & finds JSON; option.WithMaxRetries used |
| 21 | Phase 3 — LLM client (`internal/llm`) | 單元測試：httptest mock SDK、各種 JSON 包裝/錯誤格式、必填欄位驗證、cache_control marshalling 驗證 | [x] | done | internal/llm/llm_test.go covers httptest happy-path, ParseOutput variants, cache_control marshalling verification |
| 22 | Phase 4 — Prompt builder (`internal/prompt`) | `func Build(change *arceus.Change, diff *gitdiff.Diff) (system string, segments []llm.Segment)` | [x] | done | internal/prompt/prompt.go Build(change, diff) returns (system string, []llm.Segment) |
| 23 | Phase 4 — Prompt builder (`internal/prompt`) | System prompt：角色定義 + JSON schema + verdict 規則 | [x] | done | SystemPrompt constant contains role + JSON schema + verdict rules |
| 24 | Phase 4 — Prompt builder (`internal/prompt`) | User prompt：XML-ish 分段（proposal / spec / tasks / decisions / git_diff），indexed tasks & criteria 列表 | [x] | done | renderProposal/Spec/Tasks/Decisions/Diff emit XML-ish tags; indexed lists rendered |
| 25 | Phase 4 — Prompt builder (`internal/prompt`) | 對 proposal+spec 段加入 Cache=true（透過 LLM segment 機制，下游 anthropic.go 翻成 cache_control） | [x] | done | Build returns segments[0].Cache=true (static), segments[1].Cache=false (dynamic) |
| 26 | Phase 4 — Prompt builder (`internal/prompt`) | 單元測試：system prompt 形狀、二段切割、cache flag、索引項渲染、binary/omitted/truncated 標記 | [x] | done | internal/prompt/prompt_test.go covers shape, two-segment split, cache flag, indexed rendering, binary/omitted/truncated markers |
| 27 | Phase 5 — Report renderer (`internal/report`) | 定義 `Report` struct（對應 spec.md 「報告格式」段） | [x] | done | internal/report/report.go Report struct with all spec'd fields |
| 28 | Phase 5 — Report renderer (`internal/report`) | `func RenderMarkdown(r Report) string` | [x] | done | RenderMarkdown produces sections matching spec.md report format |
| 29 | Phase 5 — Report renderer (`internal/report`) | `func RenderJSON(r Report) ([]byte, error)`，含 `internal/report/schema.json` | [x] | done | RenderJSON + internal/report/schema.json present |
| 30 | Phase 5 — Report renderer (`internal/report`) | 處理空 case：無 task / 無 acceptance criteria / 無 drift 時仍能渲染 | [x] | done | RenderMarkdown emits '_No task verdicts returned._' / '_No drift detected._' for empty cases |
| 31 | Phase 5 — Report renderer (`internal/report`) | `From()` 將 LLM Output 與 arceus.Change 依索引 join，未匹配索引保留資料（避免幻覺資料消失） | [x] | done | From() joins by index map; hallucinated indices preserved with empty Text (TestFrom_UnknownIndexPreserved) |
| 32 | Phase 5 — Report renderer (`internal/report`) | `HasFindings()` 給 CLI exit code 用 | [x] | done | Report.HasFindings() inspects tasks/criteria/drift |
| 33 | Phase 5 — Report renderer (`internal/report`) | 單元測試：成節判定、markdown 內容、pipe 字元 escape、JSON round-trip、empty cases | [x] | done | internal/report/report_test.go covers all required cases including pipe escaping and JSON round-trip |
| 34 | Phase 6 — CLI wiring (`cmd/check-spec`) | cobra root command + 子命令 `verify` / `list` / `version` | [x] | done | internal/cli/root.go adds verify/list/version subcommands via cobra |
| 35 | Phase 6 — CLI wiring (`cmd/check-spec`) | `verify` 串接：arceus.Load → gitdiff.Collect → prompt.Build → llm.Verify → report.Render → output | [x] | done | internal/cli/verify.go runVerify wires Load→Collect→Build→Verify→From→write |
| 36 | Phase 6 — CLI wiring (`cmd/check-spec`) | Exit codes：0 / 1 (drift) / 2 (error) — 對應 spec.md 定義 | [x] | done | ExitOK/ExitFindings/ExitError constants; errFindings sentinel returns ExitFindings |
| 37 | Phase 6 — CLI wiring (`cmd/check-spec`) | 用 `slog` 結構化 log，預設 INFO，`--verbose` 切 DEBUG | [x] | done | root.go PersistentPreRun installs slog handler at INFO/DEBUG based on --verbose |
| 38 | Phase 6 — CLI wiring (`cmd/check-spec`) | E2E 測試：FakeJudge 跑 `verify`、pass/drift/not-found/json/output-file/invalid-format/judge-error/list/version 共 10 個 E2E 通過 | [x] | done | internal/cli/verify_test.go contains 10 E2E tests covering pass/drift/not-found/json/output-file/invalid-format/judge-error/list/empty-list/version |
| 39 | Phase 6 — CLI wiring (`cmd/check-spec`) | App struct 設計讓 JudgeFactory 可注入（測試可注入 FakeJudge，prod 用 AnthropicJudge） | [x] | done | App struct with JudgeFactory field; tests use newAppWithFakeJudge |
| 40 | Phase 7 — GitHub Action | `action.yml`（composite action）放在 repo 根目錄 | [x] | done | action.yml at repo root, using: composite |
| 41 | Phase 7 — GitHub Action | 步驟：download release binary（gh release download）→ run `check-spec verify` → `marocchino/sticky-pull-request-comment` 貼 comment | [x] | done | action.yml steps: gh release download → check-spec verify → marocchino/sticky-pull-request-comment@v2 |
| 42 | Phase 7 — GitHub Action | 輸入：`change-id`、`base-ref`、`head-ref`、`model`、`max-diff-bytes`、`anthropic-api-key`、`check-spec-version`、`post-comment`、`fail-on-findings` | [x] | done | action.yml inputs section enumerates all 9 listed inputs |
| 43 | Phase 7 — GitHub Action | 自動偵測邏輯：`git diff --name-only base..head` 過濾 `.arceus/changes/<id>/` | [x] | done | action.yml 'Auto-detect change-id' step uses git diff --name-only and awk on .arceus/changes/ |
| 44 | Phase 7 — GitHub Action | 範例 workflow `.github/workflows/check-spec-example.yml`，並在 README 引用 | [x] | done | .github/workflows/check-spec-example.yml present; README references mikeqoo1/check-spec@v0 |
| 45 | Phase 7 — GitHub Action | `actionlint` 加入 CI workflow（本機沒裝，靠 CI raven-actions/actionlint@v2 驗證） | [x] | done | ci.yml includes 'Lint GitHub Action manifest' step using raven-actions/actionlint@v2 |
| 46 | Phase 8 — Release & docs | GoReleaser config：`.goreleaser.yml`，linux/darwin/windows × amd64/arm64 + checksums | [x] | done | .goreleaser.yml configures linux/darwin/windows × amd64/arm64 with checksum block |
| 47 | Phase 8 — Release & docs | `.github/workflows/release.yml`：on tag `v*` 跑 goreleaser | [x] | done | .github/workflows/release.yml runs goreleaser on tag v* |
| 48 | Phase 8 — Release & docs | README：安裝、最小可用範例、CI 範例、報告結構、隱私聲明、CLI reference、架構說明 | [x] | done | README.md expanded to include install, quick start, report structure, GitHub Action, CLI reference, how-it-works, privacy, development |
| 49 | Phase 8 — Release & docs | 在本 repo dogfood：對本 change 跑一次 verify → `docs/audits/v0.1-self-audit.md`（需 `ANTHROPIC_API_KEY`，由使用者執行） | [ ] | missing | docs/audits/v0.1-self-audit.md exists in diff but is a hand-authored/illustrative report based on the empty stub state (BaseSHA 4b825dc empty tree); it explicitly says nearly everything is missing — this is not a real dogfood run of the final implementation |
|   |   |   |   | **notes** | Task description says 'in repo dogfood: run verify against this change'. The committed file documents an earlier/zero-state audit, not a run against the implemented code. Per tasks.md this is reported [ ] / pending user execution, so missing is consistent with self-report. |
| 50 | Phase 9 — Acceptance gate | 走查 spec.md「驗收條件」每一項，逐條打勾並貼證據（檔案路徑 / 測試名稱） | [x] | done | spec.md acceptance section in diff shows all 10 verifiable criteria checked with evidence notes; only #11 (dogfood) remains [ ] |
| 51 | Phase 9 — Acceptance gate | 將本 change `status` 從 `active` 轉為 `completed`：等使用者確認 dogfood 後執行 `arceus change status <id> completed` | [ ] | missing | meta.json shows status:'active'; no transition to 'completed' in this diff |
|   |   |   |   | **notes** | Self-reported as pending; consistent. |

## Acceptance criteria (from spec.md)

- **PASS**: criterion 1 — `check-spec verify --change <id>` 在本機可跑通，產出 markdown 到 stdout — `TestVerify_E2E_PassExit0` + smoke test
  - evidence: internal/cli/verify_test.go TestVerify_E2E_PassExit0 covers stdout markdown output
- **PASS**: criterion 2 — `--format json` 輸出符合 `internal/report/schema.json` 定義 — `TestVerify_E2E_JSONFormat`
  - evidence: TestVerify_E2E_JSONFormat asserts JSON output validity; schema.json present at internal/report/schema.json
  - notes: Test verifies parseable JSON and key fields but does not validate against the JSON Schema; spec says 'conforms to schema' which is loosely met.
- **PASS**: criterion 3 — 缺少 `ANTHROPIC_API_KEY` 時回傳 exit 2 並印出明確錯誤訊息 — `defaultJudgeFactory` 檢查 env，`TestVerify_JudgeFactoryError` 覆蓋
  - evidence: internal/cli/verify.go defaultJudgeFactory checks ANTHROPIC_API_KEY env; TestVerify_JudgeFactoryError covers factory error path
- **PASS**: criterion 4 — 找不到 `.arceus/changes/<id>/` 時回傳 exit 2 並列出可用 id — `TestVerify_E2E_ChangeNotFound`
  - evidence: verify.go errors.Is(ErrChangeNotFound) branch lists available ids via arceus.List; TestVerify_E2E_ChangeNotFound asserts 'real-change' appears in stderr
- **PASS**: criterion 5 — 對 sample-change-pass 跑（mocked LLM）產出含 verdict APPROVE、無 drift — `TestVerify_E2E_PassExit0`（用 sanitizeReport 處理 SHA 浮動）
  - evidence: TestVerify_E2E_PassExit0 asserts APPROVE verdict, '_No drift detected._' substring, and uses sanitizeReport
- **PASS**: criterion 6 — 對 sample-change-drift 跑（mocked LLM）產生 verdict=`REQUEST_CHANGES` 並列出 drift — `TestVerify_E2E_DriftExit1`
  - evidence: TestVerify_E2E_DriftExit1 asserts REQUEST_CHANGES verdict + Undocumented additions section
- **PASS**: criterion 7 — `go test ./...` 全綠 — 6 packages、44+ 個測試通過（含 -race）
  - evidence: All listed _test.go files present across 6 packages (arceus, gitdiff, llm, prompt, report, cli); CI workflow runs go test -race
  - notes: Cannot execute tests in audit; verifying structural presence.
- **PASS**: criterion 8 — `golangci-lint run` 無錯誤 — 0 issues（v2.12.2，啟用 errcheck/govet/staticcheck/gosec/revive/ineffassign/unused + gofmt/goimports）
  - evidence: .golangci.yml v2 config with required linters enabled; CI workflow runs golangci-lint
  - notes: Cannot execute lint; structural verification only.
- **PASS**: criterion 9 — GitHub Action 的 `action.yml` 由 `raven-actions/actionlint@v2` 在 CI 驗證
  - evidence: ci.yml 'Lint GitHub Action manifest' step uses raven-actions/actionlint@v2
- **PASS**: criterion 10 — README 含安裝、最小可用範例、CI 範例、報告結構、隱私聲明、CLI reference、架構說明
  - evidence: README.md diff contains Install, Quick start, GitHub Action, Report structure, Privacy note, CLI reference, How it works (architecture) sections
- **FAIL**: criterion 11 — 在 `check_spec` 自己的 repo 上 dogfood：對本 change 跑一次 verify — 待使用者執行 `make build && ./check-spec verify ...`（程序載於 `docs/audits/v0.1-self-audit.md`）
  - evidence: docs/audits/v0.1-self-audit.md present but it's a pre-implementation/zero-state audit (base 4b825dc empty tree, head 1e0254e), not a verify run against the implemented code
  - notes: Tasks self-report this as [ ] — consistent — but the criterion itself remains unmet.

## Drift findings

**Undocumented additions** (in diff, not in spec):

- docs/audits/v0.1-self-audit.md committed even though tasks.md lists it as user-pending — but content reflects an empty-state audit rather than a real dogfood; could mislead readers
- go.sum includes generic-list-go, jsonparser, invopop/jsonschema and other indirect deps brought in by anthropic-sdk-go (expected transitive)
- action.yml/goreleaser archives reference LICENSE file but LICENSE is not visible in this diff (may already be in repo from prior commit)

**Missing from implementation** (in spec, not in diff):

- Real dogfood self-audit (criterion 11) — see task 49
- Change status transition from active→completed (task 51)

## Open questions for human reviewer

- Should docs/audits/v0.1-self-audit.md be removed or regenerated against the actual implementation before the change is marked completed?
- Is LICENSE already committed in a prior commit (so .goreleaser.yml archive reference is valid), or does it need to be added?
- The JSON output test only checks structural validity, not conformance to internal/report/schema.json — is informal conformance acceptable for v0.1, or should the test load the schema?

