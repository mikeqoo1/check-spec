# Spec/Code Consistency Audit — 2026-05-20-v01-mvp-llm-judged-speccode-consistency-auditor-go-cli-githu

_v0.1 MVP — LLM-judged spec/code consistency auditor (Go CLI + GitHub Action)_

- **Verdict**: REQUEST_CHANGES
- **Model**: claude-opus-4-7
- **Base → Head**: 4b825dc642cb6eb9a060e54bf8d69288fbee4904 (4b825dc) → HEAD (1e0254e)
- **Files analyzed**: 3

## Summary

The proposal/spec/tasks promise a full Go CLI (`check-spec`) plus GitHub Action implementation across 9 phases (arceus loader, gitdiff collector, LLM judge, prompt builder, report renderer, CLI wiring, action.yml, GoReleaser, README). However, the provided git diff contains only three files: `.gitignore`, `LICENSE`, and a two-line `README.md` stub (`# check_spec\n監督阿爾宙斯的計畫`). None of the Go source code, tests, Makefile, golangci config, CI workflows, action.yml, GoReleaser config, internal packages, or testdata exist in the diff. Nearly every task marked `[x]` is unverifiable/absent in the diff.

## Task implementation (from tasks.md)

| # | Phase | Task | Reported | Actual | Evidence |
|---|-------|------|----------|--------|----------|
| 1 | Phase 0 — Repo bootstrap | 初始化 `go mod init github.com/mikeqoo1/check-spec` | [x] | missing | no go.mod in diff |
| 2 | Phase 0 — Repo bootstrap | 建立目錄骨架：`cmd/check-spec/`、`internal/arceus/`、`internal/gitdiff/`、`internal/llm/`、`internal/report/`、`internal/prompt/`、`testdata/` | [x] | missing | no cmd/ or internal/ directories in diff |
| 3 | Phase 0 — Repo bootstrap | 加入 `.golangci.yml`（啟用 `errcheck`、`govet`、`staticcheck`、`gosec`、`revive`） | [x] | missing | no .golangci.yml in diff |
| 4 | Phase 0 — Repo bootstrap | 加入 `Makefile`：`make build`、`make test`、`make lint`、`make verify` | [x] | missing | no Makefile in diff |
| 5 | Phase 0 — Repo bootstrap | 加入 `.github/workflows/ci.yml`：on push/PR 跑 `make verify` | [x] | missing | no .github/workflows/ci.yml in diff |
| 6 | Phase 1 — Arceus loader (`internal/arceus`) | `type Change struct { ID, Title string; Proposal, Spec, Tasks, Decisions string; Meta MetaJSON }` | [x] | missing | no internal/arceus/*.go in diff |
| 7 | Phase 1 — Arceus loader (`internal/arceus`) | `func Load(repoRoot, changeID string) (*Change, error)` — 讀 `.arceus/changes/<id>/{proposal,spec,tasks,decisions}.md` + `meta.json`，缺檔回 error | [x] | missing | no Load function in diff |
| 8 | Phase 1 — Arceus loader (`internal/arceus`) | `func List(repoRoot string) ([]string, error)` — 列出可用 change id（含 archive 排除） | [x] | missing | no List function in diff |
| 9 | Phase 1 — Arceus loader (`internal/arceus`) | 解析 `tasks.md` 為 `[]Task{Index int, Text string, Checked bool, Phase string}`（保留原始 markdown 也保留結構化） | [x] | missing | no tasks.md parser in diff |
| 10 | Phase 1 — Arceus loader (`internal/arceus`) | 解析 `spec.md` 的「驗收條件」段：找 `## 驗收條件` 或 `## Acceptance criteria` heading，收集底下 `- [ ]` / `- [x]` 行 | [x] | missing | no spec parser in diff |
| 11 | Phase 1 — Arceus loader (`internal/arceus`) | 單元測試覆蓋：正常、缺檔、空 acceptance section、混合中英文 heading | [x] | missing | no tests in diff |
| 12 | Phase 2 — Git diff collector (`internal/gitdiff`) | `func Collect(repoRoot, base, head string, maxBytes int) (Diff, error)`，回傳 `Diff{Files []FileDiff, Truncated bool}` | [x] | missing | no internal/gitdiff in diff |
| 13 | Phase 2 — Git diff collector (`internal/gitdiff`) | 用 `exec.CommandContext` 跑 `git diff --no-color <base>..<head>` 與 `git diff --name-only`；不可信任使用者輸入 → 預先 validate ref 不含 shell metacharacters | [x] | missing | no exec.CommandContext usage in diff |
| 14 | Phase 2 — Git diff collector (`internal/gitdiff`) | 排除 binary file（從 `git diff --numstat` 的 `-` 標記識別） | [x] | missing | no numstat handling in diff |
| 15 | Phase 2 — Git diff collector (`internal/gitdiff`) | 超過 `maxBytes` 時：按 file 切，優先保留 spec/code 相關的非 vendor、非生成檔 | [x] | missing | no truncation logic in diff |
| 16 | Phase 2 — Git diff collector (`internal/gitdiff`) | 單元測試：使用 `git init` 在 tmp dir 起的 fixture repo，覆蓋一般 diff、binary file 過濾、截斷情況 | [x] | missing | no gitdiff tests in diff |
| 17 | Phase 3 — LLM client (`internal/llm`) | 定義介面：`type Judge interface { Verify(ctx, JudgeInput) (JudgeOutput, error) }` | [x] | missing | no internal/llm in diff |
| 18 | Phase 3 — LLM client (`internal/llm`) | Anthropic 實作 `AnthropicJudge`，使用官方 SDK；spec.md / proposal.md 段落用 `cache_control: ephemeral` 啟用 prompt caching | [x] | missing | no AnthropicJudge in diff |
| 19 | Phase 3 — LLM client (`internal/llm`) | Mock 實作 `FakeJudge`：固定 Output 回應（測試與 golden file 用） | [x] | missing | no FakeJudge in diff |
| 20 | Phase 3 — LLM client (`internal/llm`) | `Verify` 要求模型回 JSON；SDK 內建重試機制處理 429/5xx；malformed JSON 由 `ParseOutput` 容錯（去 code fence + 找 `{...}`） | [x] | missing | no ParseOutput in diff |
| 21 | Phase 3 — LLM client (`internal/llm`) | 單元測試：httptest mock SDK、各種 JSON 包裝/錯誤格式、必填欄位驗證、cache_control marshalling 驗證 | [x] | missing | no llm tests in diff |
| 22 | Phase 4 — Prompt builder (`internal/prompt`) | `func Build(change *arceus.Change, diff *gitdiff.Diff) (system string, segments []llm.Segment)` | [x] | missing | no internal/prompt in diff |
| 23 | Phase 4 — Prompt builder (`internal/prompt`) | System prompt：角色定義 + JSON schema + verdict 規則 | [x] | missing | no system prompt code in diff |
| 24 | Phase 4 — Prompt builder (`internal/prompt`) | User prompt：XML-ish 分段（proposal / spec / tasks / decisions / git_diff），indexed tasks & criteria 列表 | [x] | missing | no user prompt builder in diff |
| 25 | Phase 4 — Prompt builder (`internal/prompt`) | 對 proposal+spec 段加入 Cache=true（透過 LLM segment 機制，下游 anthropic.go 翻成 cache_control） | [x] | missing | no cache flag handling in diff |
| 26 | Phase 4 — Prompt builder (`internal/prompt`) | 單元測試：system prompt 形狀、二段切割、cache flag、索引項渲染、binary/omitted/truncated 標記 | [x] | missing | no prompt tests in diff |
| 27 | Phase 5 — Report renderer (`internal/report`) | 定義 `Report` struct（對應 spec.md 「報告格式」段） | [x] | missing | no internal/report in diff |
| 28 | Phase 5 — Report renderer (`internal/report`) | `func RenderMarkdown(r Report) string` | [x] | missing | no RenderMarkdown in diff |
| 29 | Phase 5 — Report renderer (`internal/report`) | `func RenderJSON(r Report) ([]byte, error)`，含 `internal/report/schema.json` | [x] | missing | no schema.json in diff |
| 30 | Phase 5 — Report renderer (`internal/report`) | 處理空 case：無 task / 無 acceptance criteria / 無 drift 時仍能渲染 | [x] | missing | no report renderer in diff |
| 31 | Phase 5 — Report renderer (`internal/report`) | `From()` 將 LLM Output 與 arceus.Change 依索引 join，未匹配索引保留資料（避免幻覺資料消失） | [x] | missing | no From() in diff |
| 32 | Phase 5 — Report renderer (`internal/report`) | `HasFindings()` 給 CLI exit code 用 | [x] | missing | no HasFindings in diff |
| 33 | Phase 5 — Report renderer (`internal/report`) | 單元測試：成節判定、markdown 內容、pipe 字元 escape、JSON round-trip、empty cases | [x] | missing | no report tests in diff |
| 34 | Phase 6 — CLI wiring (`cmd/check-spec`) | cobra root command + 子命令 `verify` / `list` / `version` | [x] | missing | no cmd/check-spec in diff |
| 35 | Phase 6 — CLI wiring (`cmd/check-spec`) | `verify` 串接：arceus.Load → gitdiff.Collect → prompt.Build → llm.Verify → report.Render → output | [x] | missing | no verify command wiring in diff |
| 36 | Phase 6 — CLI wiring (`cmd/check-spec`) | Exit codes：0 / 1 (drift) / 2 (error) — 對應 spec.md 定義 | [x] | missing | no exit code logic in diff |
| 37 | Phase 6 — CLI wiring (`cmd/check-spec`) | 用 `slog` 結構化 log，預設 INFO，`--verbose` 切 DEBUG | [x] | missing | no slog usage in diff |
| 38 | Phase 6 — CLI wiring (`cmd/check-spec`) | E2E 測試：FakeJudge 跑 `verify`、pass/drift/not-found/json/output-file/invalid-format/judge-error/list/version 共 10 個 E2E 通過 | [x] | missing | no E2E tests in diff |
| 39 | Phase 6 — CLI wiring (`cmd/check-spec`) | App struct 設計讓 JudgeFactory 可注入（測試可注入 FakeJudge，prod 用 AnthropicJudge） | [x] | missing | no App struct in diff |
| 40 | Phase 7 — GitHub Action | `action.yml`（composite action）放在 repo 根目錄 | [x] | missing | no action.yml in diff |
| 41 | Phase 7 — GitHub Action | 步驟：download release binary（gh release download）→ run `check-spec verify` → `marocchino/sticky-pull-request-comment` 貼 comment | [x] | missing | no composite action steps in diff |
| 42 | Phase 7 — GitHub Action | 輸入：`change-id`、`base-ref`、`head-ref`、`model`、`max-diff-bytes`、`anthropic-api-key`、`check-spec-version`、`post-comment`、`fail-on-findings` | [x] | missing | no action inputs in diff |
| 43 | Phase 7 — GitHub Action | 自動偵測邏輯：`git diff --name-only base..head` 過濾 `.arceus/changes/<id>/` | [x] | missing | no auto-detect logic in diff |
| 44 | Phase 7 — GitHub Action | 範例 workflow `.github/workflows/check-spec-example.yml`，並在 README 引用 | [x] | missing | no example workflow in diff |
| 45 | Phase 7 — GitHub Action | `actionlint` 加入 CI workflow（本機沒裝，靠 CI raven-actions/actionlint@v2 驗證） | [x] | missing | no actionlint workflow in diff |
| 46 | Phase 8 — Release & docs | GoReleaser config：`.goreleaser.yml`，linux/darwin/windows × amd64/arm64 + checksums | [x] | missing | no .goreleaser.yml in diff |
| 47 | Phase 8 — Release & docs | `.github/workflows/release.yml`：on tag `v*` 跑 goreleaser | [x] | missing | no release.yml in diff |
| 48 | Phase 8 — Release & docs | README：安裝、最小可用範例、CI 範例、報告結構、隱私聲明、CLI reference、架構說明 | [x] | missing | README.md:1-2 only contains title and a single Chinese line; no install/CI/CLI reference/privacy/architecture content |
| 49 | Phase 8 — Release & docs | 在本 repo dogfood：對本 change 跑一次 verify → `docs/audits/v0.1-self-audit.md`（需 `ANTHROPIC_API_KEY`，由使用者執行） | [ ] | missing | no docs/audits/v0.1-self-audit.md in diff |
| 50 | Phase 9 — Acceptance gate | 走查 spec.md「驗收條件」每一項，逐條打勾並貼證據（檔案路徑 / 測試名稱） | [x] | missing | no evidence file with criterion walk-through in diff |
| 51 | Phase 9 — Acceptance gate | 將本 change `status` 從 `active` 轉為 `completed`：等使用者確認 dogfood 後執行 `arceus change status <id> completed` | [ ] | missing | status change not reflected in diff |

## Acceptance criteria (from spec.md)

- **FAIL**: criterion 1 — `check-spec verify --change <id>` 在本機可跑通，產出 markdown 到 stdout — `TestVerify_E2E_PassExit0` + smoke test
  - evidence: no Go binary or test in diff
- **FAIL**: criterion 2 — `--format json` 輸出符合 `internal/report/schema.json` 定義 — `TestVerify_E2E_JSONFormat`
  - evidence: no schema.json or JSON renderer in diff
- **FAIL**: criterion 3 — 缺少 `ANTHROPIC_API_KEY` 時回傳 exit 2 並印出明確錯誤訊息 — `defaultJudgeFactory` 檢查 env，`TestVerify_JudgeFactoryError` 覆蓋
  - evidence: no defaultJudgeFactory implementation in diff
- **FAIL**: criterion 4 — 找不到 `.arceus/changes/<id>/` 時回傳 exit 2 並列出可用 id — `TestVerify_E2E_ChangeNotFound`
  - evidence: no CLI implementation in diff
- **FAIL**: criterion 5 — 對 sample-change-pass 跑（mocked LLM）產出含 verdict APPROVE、無 drift — `TestVerify_E2E_PassExit0`（用 sanitizeReport 處理 SHA 浮動）
  - evidence: no E2E test or sample-change-pass fixture in diff
- **FAIL**: criterion 6 — 對 sample-change-drift 跑（mocked LLM）產生 verdict=`REQUEST_CHANGES` 並列出 drift — `TestVerify_E2E_DriftExit1`
  - evidence: no E2E test or sample-change-drift fixture in diff
- **FAIL**: criterion 7 — `go test ./...` 全綠 — 6 packages、44+ 個測試通過（含 -race）
  - evidence: no Go test files in diff
- **FAIL**: criterion 8 — `golangci-lint run` 無錯誤 — 0 issues（v2.12.2，啟用 errcheck/govet/staticcheck/gosec/revive/ineffassign/unused + gofmt/goimports）
  - evidence: no .golangci.yml in diff
- **FAIL**: criterion 9 — GitHub Action 的 `action.yml` 由 `raven-actions/actionlint@v2` 在 CI 驗證
  - evidence: no action.yml or actionlint workflow in diff
- **FAIL**: criterion 10 — README 含安裝、最小可用範例、CI 範例、報告結構、隱私聲明、CLI reference、架構說明
  - evidence: README.md is a 2-line stub: '# check_spec\n監督阿爾宙斯的計畫'
- **FAIL**: criterion 11 — 在 `check_spec` 自己的 repo 上 dogfood：對本 change 跑一次 verify — 待使用者執行 `make build && ./check-spec verify ...`（程序載於 `docs/audits/v0.1-self-audit.md`）
  - evidence: no dogfood audit document in diff

## Drift findings

**Undocumented additions** (in diff, not in spec):

- LICENSE (Apache-2.0) added but not mentioned in spec/tasks

**Missing from implementation** (in spec, not in diff):

- Entire Go CLI implementation (cmd/, internal/arceus, internal/gitdiff, internal/llm, internal/prompt, internal/report)
- Makefile, .golangci.yml, .goreleaser.yml
- GitHub Actions: ci.yml, release.yml, action.yml, example workflow
- README content (install/CI/privacy/architecture/CLI reference)
- testdata fixtures and all tests
- docs/audits/v0.1-self-audit.md

## Open questions for human reviewer

- Was the diff truncated, or does this repo genuinely contain only .gitignore, LICENSE, and a stub README? The tasks.md self-reports nearly everything as done with specific test names, but no source files appear in the diff. Please confirm whether the implementation was committed to a different branch/path or whether the checkbox state in tasks.md is inaccurate.

